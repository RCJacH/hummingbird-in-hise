namespace CCDefault {
  const var vibratoDepth = 1;
  const var volumeReleases = 9;
  const var pickingDirection = 13;
  const var frettingPosition = 14;
  const var volumeNoises = 15;
  const var sympatheticResonance = 16;
  const var strumSpeed = 17;
  const var strumAccelleration = 18;
  const var vibratoSpeed = 19;
  const var silent = 64;
  const var palmMute = 67;
  const var humanize = 100;
}

namespace CCs {
  reg vibratoDepth = CCDefault.vibratoDepth;
  reg volumeReleases = CCDefault.volumeReleases;
  reg pickingDirection = CCDefault.pickingDirection;
  reg frettingPosition = CCDefault.frettingPosition;
  reg volumeNoises = CCDefault.volumeNoises;
  reg sympatheticResonance = CCDefault.sympatheticResonance;
  reg strumSpeed = CCDefault.strumSpeed;
  reg strumAccelleration = CCDefault.strumAccelleration;
  reg vibratoSpeed = CCDefault.vibratoSpeed;
  reg humanize = CCDefault.humanize;
  reg silent = CCDefault.silent;
  reg palmMute = CCDefault.palmMute;
}
